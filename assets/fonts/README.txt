Place your LICENSED font files here so the page renders the exact design type.

TT Norms Pro (body / sans) — used for: eyebrow labels (Қыз Ұзату, Той иелері,
Мекен-жайымыз, Тойға дейін, Сауалнама), greeting, body text, date block,
calendar (weekday labels + day numbers), countdown numbers + labels, the RSVP
form (labels, inputs, buttons).
  Expected files:
    TTNormsPro-Light.woff2     (300)
    TTNormsPro-Regular.woff2   (400)
    TTNormsPro-Medium.woff2    (500)
    TTNormsPro-DemiBold.woff2  (600)
  (.woff also accepted as a fallback source)

Alistair Signature (script) — used for: the big "Елдана", "Нұржан-Айнұр",
"Paris hall", "Тамыз", "аз уақыт қалды", the thank-you heading, and the intro name.
  Expected file:
    AlistairSignature.woff2    (.woff also accepted)

Until these are added, the page falls back to Manrope (for TT Norms Pro) and
Marck Script (for Alistair Signature), which are close, Cyrillic-capable matches.
